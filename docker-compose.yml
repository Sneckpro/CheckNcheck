services:
  bot:
    build: .
    container_name: expense-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/expenses.db
    volumes:
      - expense-data:/app/data

volumes:
  expense-data:
